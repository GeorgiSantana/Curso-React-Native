const converterInt=(e) =>parseInt(e);
const dobrar = (e)=> e * 2;
let num = ['1', '2', '3', '4', '5', '6'].map(dobrar);

console.log(num);


//const curso = ['HTML', 'CSS', 'REACT', 'JavaScript'];

// curso.map((elemento, indice) => {
// console.log('Curso ' + elemento + ' - Posição do curso ' + indice);
// });

// let c = curso.map((elemento, indice) => {
//   return {elemento, indice};
// });

// console.log(c);